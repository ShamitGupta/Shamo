fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
supabase>=2.5
openai>=1.40
httpx>=0.27
truststore>=0.9
pytest>=8.0

# Manim (Visualize's animated-region template). manimpango ships Pango/Cairo
# as prebuilt wheels, and imageio-ffmpeg bundles a static ffmpeg binary, so
# `pip install` alone is enough on Windows -- no system Cairo/Pango/ffmpeg
# install is required. Confirmed with backend_v2/tests/live_manim_smoke.py.
manim>=0.19,<0.22
imageio-ffmpeg>=0.5
